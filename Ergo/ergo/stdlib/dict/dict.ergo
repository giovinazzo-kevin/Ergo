:- module(dict, [
	'$'/2
]).

:- use_module(list).

 %: TODO: Fix parsing and replace this operator with '.'/2
:- op(900, xfy, ['$']).

:- expand([O] >> (dict(Tag, Args)$Key :- dict_key_value_(dict(Tag, Args), Key, O))).

dict_key_value_(dict(Tag, Args), Key, Eval) :- 
	dict_key_value(dict(Tag, Args), Key, Eval), !.
