:- module(info, [
]).

:- expand([O] >> (culo :- O = ano)).
:- expand([O] >> (culo :- O = chiappa)).
:- expand([O] >> (figa :- O = tette)).
:- expand([O] >> (figa :- O = sesso)).
:- expand([O] >> (test(X) :- O = tost)).

test :- test(culo), figa, pokemon.