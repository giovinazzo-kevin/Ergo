:- module(dict, [
	'='/2
]).

:- op(900, yfx, ['.']).

D.Key = Val :- 
	ground(D),
	% Recursively expands itself to handle nested dictionaries!
	D = dict(Tag, Args), 
	dict_key_value(dict(Tag, Args), Key, Val), 
	% Cut to prevent regular unification from providing another solution
	!.