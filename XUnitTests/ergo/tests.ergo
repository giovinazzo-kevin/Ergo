:- module(tests, [
	fail/0, succeed/0
]).
:- use_module(case01).
:- use_module(case02).
:- use_module(case03).
:- use_module(case04).
:- use_module(case05).
:- dynamic(t/0).
:- dynamic(t/1).

fail :- false.
succeed.

tail_recurse(0) :- !.
tail_recurse(X) :- 
	X > 0, 
	Y := X - 1, 
	tail_recurse(Y).