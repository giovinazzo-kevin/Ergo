:- module(tree, [
	tree/1
]).

:- use_module(option).

tree(void).
tree(Node) :-
  named_tuple(Node, node, (_Element,Left,Right)),
  named_arg(_Element, value, _),
  named_arg(Left, left, L),
  named_arg(Right, right, R),
  tree(L), tree(R).